## Prism JSON Comparitor

Compare two Prism Phases with what affected hosts are missing from the first JSON file with the second JSON file.

How to use

`./prismDifference originalScan.json latestScan.json`
